
@using Microsoft.AspNetCore.Http
<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        
                        @if (Context.Session.GetString("username") != "" || Context.Session.GetString("username") != null || Context.Session.GetString("username") != "undefine")
                        {
                            <span class="block m-t-xs font-bold">@Context.Session.GetString("username")</span>
                            <span class="text-muted text-xs block">@User.FindFirst("rolename").Value <b class="caret"></b></span>
                        }
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs" style="position: absolute; top: 91px; left: 0px; will-change: top, left;">
                        <li><a class="dropdown-item" href="@Url.Action("Logout", "account")">Logout</a></li>
                    </ul>
                </div>
                <div class="logo-element">

                </div>
            </li>
            @if (User.FindFirst("rolename").Value == "Admin")
            {
            <li class="@Html.IsSelected(controller: "category", action: "category")">
                <a href="#">
                    <i class="fa fa-table"></i>
                    @*<img src="~/lib/bootstrap-icons-1.3.0/table.svg" />*@
                    <span class="fa arrow"></span>
                    <span class="nav-label">Category</span>
                </a>
                <ul class="nav nav-second-level collapse">
                    <li style="padding-left: 30px;padding-bottom: 20px;"><span class="fa fa-th-list" style="margin-right: 10px;"></span><a href="@Url.Action("category","category")" style="display: contents;">Category List</a></li>
                    <li style=" padding-left: 30px;"><span class="fa fa-plus" style="margin-right: 10px;"></span><a href="#" style="display: contents;">Add Category</a></li>
                </ul>
            </li>
            }
                <li class="@Html.IsSelected(controller: "Home", action: "Main")">
                    <a href="@Url.Action("Main", "Home")"><i class="fa fa-diamond"></i></a>
                </li>
                <li class="@Html.IsSelected(controller: "Home", action: "Minor")">
                    <a href="@Url.Action("Minor", "Home")"><i class="fa fa-server"></i></a>
                </li>
            </ul>
    </div>
</nav>